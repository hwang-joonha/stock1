# 투자심사 모델링 레포

주식 종목의 투자심사 보고서를, 문서가 아니라 **인터랙티브 추정 모델**로 만든다.
심사 판단(밸류에이션·시나리오·투자의견)이 모델의 뷰로 들어가므로
보고서와 모델의 숫자가 어긋날 수 없다.

현재 대상: **삼성전기 (009150)** · 단위 **억원** · 모델 루트 **적정 시가총액**

---

## 구조

```
framework/          규약 문서 — 먼저 읽는다
  modeling_framework.md    Q×P 분해 · 객관/주관 · 검증 게이트
  html_template_spec.md    data.js 스키마 · 수식 문법 · 엔진 계약
  ic_memo_framework.md     투자심사 판단 레이어
  excel_spec.md            Excel 산출 규격
design-guide/
  tokens.js           색·타이포 단일 출처. 임의 hex 금지
templates/
  model_template.html 엔진 (약 4,000줄, 종목 무관)
tools/
  make_template.py    예시 → 템플릿 재생성 (엔진 패치가 여기 산다)
  build_model.py      템플릿 + data.js → model.html
  build_excel.py      model.html → model.xlsx (수식 내장)
  validate_model.py   검증 게이트 G1~G10
  harness.py          모델을 Node에서 평가 (브라우저 없이)
  xlsx_eval.py        xlsx 셀 수식 독립 평가 (G7용)
  dart_fetch.py       DART 공시 원문 추출
  peer_fetch.py       피어 그룹 밸류에이션 지표 수집
  extract_engine.py   HTML에서 JS 선언 추출
  fixtures/           Tesla 회귀 고정 데이터·골든값
companies/<종목>/
  data.js             손으로 쓰는 유일한 파일
  historicals.json    공시 확정값 (G1 대사용)
  model.html          빌드 산출물
examples/Tesla/       골든 샘플. 손대지 않는다
```

---

## 작업 흐름

```bash
# 1. 데이터 편집
vi companies/samsung-em/data.js

# 2. 빌드
python3 tools/build_model.py companies/samsung-em/data.js

# 3. Excel
python3 tools/build_excel.py companies/samsung-em/model.html

# 4. 검증 — 전 게이트 통과해야 함
python3 tools/validate_model.py companies/samsung-em/model.html
```

엔진을 고칠 때:

```bash
vi tools/make_template.py          # 패치를 추가한다
python3 tools/make_template.py     # 템플릿 재생성
python3 tools/validate_model.py --all   # G9가 Tesla 회귀를 잡는다
```

---

## 규칙

**`model.html`을 직접 고치지 않는다.** 빌드 산출물이라 다음 빌드에 덮인다.
엔진 수정은 `tools/make_template.py`에 패치로 넣는다 — 그래야 모든 종목이
같은 수정을 받는다.

**`examples/Tesla/`를 수정하지 않는다.** 엔진을 고칠 때마다 Tesla 산출값이
그대로 재현되는지가 회귀 기준이다 (게이트 G9, 200셀 오차 0).

**`TREE`·`INPUT_KEYS`·`DEFAULTS_S`·`GRAPH`·`SIM_SECS`를 손으로 쓰지 않는다.**
전부 `MODEL`에서 파생된다.

**색은 `design-guide/tokens.js`에서만 가져온다.** 매출 계열은 primary 램프,
비용 계열은 neutral 램프. 섞지 않는다.

**게이트 실패를 남겨두고 다음 단계로 가지 않는다.** G1·G2의 허용 오차는 0이다.

---

## 진행 상황

| Phase | 내용 | 상태 |
|---|---|---|
| 0 | 엔진 정비 — 템플릿 추출, P0 결함 수정, 검증 게이트 | 완료 |
| 1 | 매출 레이어 — 삼성전기 3개 부문 | 완료 |
| 2 | 비용 · 영업이익 | 완료 |
| 3 | 밸류에이션 · 심사 뷰 · 시나리오 · Excel · 스킬화 | 완료 |
| 4 | 정성 레이어 · 애널리스트 리포트 · 모바일 | 완료 |
| 5 | 분기 추적 · 모니터링 · 민감도 · 게이트 G11 · 2종목 검증 | 완료 |
| 6 | 개조식 어투 · 차트 · 강조 · 타이포 위계 | 완료 |
| 7 | 상반기 실적 반영 가정 재보정 · 목표배수 상향 | 완료 |
| 8 | 세 번째 종목(티엘비) · 진짜 Q×P · 엔진 결함 3건 | 완료 |
| 9 | 컨센서스 상세 대조 · 네 번째 종목(LGD, 적자 사이클) · 엔진 결함 3건 | 완료 |
| 10 | 다섯 번째 종목(삼성전자, 슈퍼사이클) — 엔진 결함 0건 | 완료 |
| 11 | 사업 구조 뷰 — 매출 구성 · 비용 성격별 · 이익 구조 · 게이트 G12 | 완료 |
| 12 | 투자자 관점 정비 — 주주 몫 레이어 · UI 동선 · 기대값 · 워치리스트 · 이력/일정 | 완료 |

### Phase 1 결과

실적 5개년(2021~2025)을 사업보고서 원문으로 확정했다. 부문합 = 연결 손익계산서
매출액이 전 연도 오차 0으로 일치하고, 게이트 G1이 20셀을 매 빌드마다 대사한다.

- 추출 도구: `tools/dart_fetch.py` (DART 검색 → 목차 → 본문)
- 원본 수치: `companies/samsung-em/dart_extract.json`
- 대사 기준: `companies/samsung-em/historicals.json`

**설계를 한 번 바꿨다.** 초안은 실적 구간까지 출하지수 × 단가지수로 쪼갤 계획이었으나,
공시가 부문별 출하 수량을 주지 않아 그 분할은 근거 없는 가정이 된다. 실적은 확정값을
그대로 쓰고, 추정 구간만 물량·단가 성장률로 나눴다. Q×P의 분석적 가치는 남기면서
없는 데이터를 지어내지 않는 지점이다.

**주의 — 회계 기준 변경**: 통신모듈 중단영업 분류로 2022·2023이 소급 재작성됐다.
차이는 전액 광학 부문(총매출의 0.19%)이며 모델은 2021~2022 최초 공시,
2023~2025 재작성 기준을 쓴다. `historicals.json`의 `_기준` 참조.

### Phase 12 결과 — 투자자 관점 정비 (5건 일괄)

주식 투자자 관점의 전면 검토에서 나온 지적을 우선순위대로 전부 실행했다.

**① 주주 몫 레이어 — 가장 큰 공백 해소.** 밸류에이션이 영업단에서 끝나
시장의 언어(PER·배당)로 번역되지 않았다. 다섯 종목의 연결포괄손익계산서·
배당 공시를 확정해 참고 체인을 얹었다: `net_income(지배) = (영업이익 +
영업외손익) × (1 − 실효세율) × 지배지분비율`, eps, dps = EPS × 배당성향.
실적은 hist 마스크로 확정값 그대로(G1 +48셀), 루트 계산에는 비투입(KT&G
ebitda 참고 노드 선례). 검증 — 삼성전자 2025 EPS 6,637원(공시 6,605원),
KT&G 10,501원(공시 10,220원). **LGD가 이 레이어의 존재 이유다**: 영업단
+49%가 이자·세금·비지배(지배 75%)를 거치면 얇아진다 — 2030 PER 3.8배로
번역된 화면이 그것을 보여준다. 티엘비는 2026 무상증자로 현 주식수(23.7M)와
공시 EPS 기준(9.7M)이 달라 desc에 기록했다.

**② UI 동선.** 첫 화면을 투자 개요로(캔버스는 도구지 첫인상이 아니다),
대시보드의 무의미한 구성비 카드(EV 99%+순차입금 1%)를 "적정가 경로 대
현재 시총" 차트로, 실적 오버라이드·마스크를 시뮬레이터와 리포트 §10에서
제외(삼성전기 슬라이더 31→19개), "하드코딩" 표기 제거, 민감도 막대에 유리
방향(±10%) 명시.

**③ 시나리오 기대값.** `MEMO.probs`([주관] 확률, G11이 합=1 검사) 선언 시
확률 가중 기대 괴리 + 상하방 배율(확률 불요) 카드. 결과가 종목을 가른다 —
삼성전기 0.6:1·기대 −16%, KT&G 0.2:1, 티엘비 1.9:1·+30%, LGD 2.1:1·+61%,
삼성전자 2.0:1·+12%.

**④ 워치리스트.** `tools/build_index.py` → 루트 `index.html`. 각 model.html을
하네스로 실행해 괴리·기대 괴리·배율·함의 PER·배당수익률·컨센서스·분기
진행률을 한 장으로. 하네스에 scenarioRoots(시나리오별 루트 해) 수출 추가.

**⑤ 이력·일정.** `MARKET.history` — 갱신·개정 때마다 (주가, 적정가) 쌍을
기록해 "모델이 주가를 뒤쫓는가"를 감시(삼성전기: −55% → −27% 재보정 이력이
첫 기록). `MEMO.events` — 법정 공시 기한·촉매 시점의 D-day 카드 (분기
모니터링, QUARTERLY 없어도 뜬다).

남은 공백(의도적 유보): 밸류에이션 교차법(종목당 단일 방법 유지), 컨센서스
리비전 추이(스냅숏 1회분뿐), FCF·운전자본 레이어(순차입금 고정 가정은
desc에 보수성 명시로 대체).

### Phase 11 결과 — 사업 구조 뷰

"이 회사가 무엇으로 돈을 버는가"가 §4의 숫자 표에 흩어져 있었다. 심사의 첫
질문을 전용 뷰로 만들어 Investment case 그룹 맨 앞에 두고, 리포트 §2에도
같은 블록을 얹었다. 캔버스가 아니라 읽는 화면이다 — 캔버스는 조작 도구이고
사업 구조는 읽을 대상이다.

- **① 매출 구성** — `META.bizMap`(부문 라벨·rev/op/cost 노드·한 줄 설명)이
  정본, 없으면 매출 합 분해에서 유도. 100% 스택(실적+추정) + 비중 변화 표
- **② 비용 성격별 구성** — 새 데이터 축. 사업보고서 **비용의 성격별 분류
  주석**을 `dart_fetch.py costnature`로 추출, `cost_nature.json` →
  `COSTNATURE` 자동 주입(quarterly와 같은 규약). 화면 전용, 모델 비투입.
  neutral 램프 스택 + 5개년 표 + 대매출 비율·pp 변화
- **③ 이익 구조** — 부문 매출비중 대 이익비중 괴리. 삼성전기: 컴포넌트가
  매출 45.9%로 이익 66.7%를 낸다(+20.8%p), 패키지는 20.3% → 14.8%

**게이트 G12.** Σ항목=합계(공시 표 자체의 반올림 잔차는 `sumTol`로 문서화 —
티엘비 ±2천원), 합계=(매출−영업이익)을 모델 노드로 대사. 주석 범위가
영업비용과 다른 해는 `check.gap`+설명 없이는 통과 못 한다. MEMO를 쓰는 모델에
COSTNATURE가 없으면 실패(PEERS 규약과 동일). 다섯 종목 275셀 대사.

**추출에서 배운 것.** 다섯 회사가 같은 주석을 네 가지로 공시한다 — XBRL
쌍줄(당기/전기 블록) 대 고전 표(구분|당기|전기), 제목 있음/없음(삼성전기
옛 보고서는 영업이익 주석 안에 무제목), 라벨도 해마다 바뀐다(LGD 외주가공비↔
외주용역비). 당기/전기 연쇄 대사가 두 건을 잡았다: 삼성전기 2023은 재작성
전후가 다르고(재작성 채택), LGD 2023은 FY2024 보고서가 주석 범위를
영업비용으로 재공시했다(재공시 채택 — gap 0). LGD 2021~2022는 옛 범위
(영업외 손상차손 포함)라 gap을 기록으로 남겼다.

### Phase 10 결과 — 삼성전자 (005930), 슈퍼사이클 정점의 추정

다섯 번째 종목. 새로 밟은 것: **부문합 ≠ 연결**(내부거래 조정이 별도 열),
**부문 실적의 음수 연도**(DS 2023 영업이익 −14.9조 — `IF(actual > 0)` 관용구가
깨져 KT&G의 `df_hist`식 **hist 마스크**를 부문 전체에 적용), 그리고
**사이클 정점의 추정**이라는 새 문제.

2026 상반기 확정이 특수하다 — 매출 305.4조(+98.7%), 영업이익 **146.7조**(2025
연간의 3.4배), DS 부문 OPM 68.3%. HBM4 슈퍼사이클의 한복판이라 2026년은 추정이
아니라 관찰이고, 심사의 실질 가정은 **정상화 경로**(DS OPM 69% → 40%)다.
2028~2029 다운사이클(−12%, −3%)을 명시적으로 넣었다.

결과: 2030 적정 시가총액 1,756조(+13%) — **다섯 종목 중 괴리가 가장 좁다**.
현재 시총이 함의하는 2030 배수 5.3배 = 본 모델 경로와 거의 일치. 시장은 이미
정상화를 가격에 반영 중이며(2026E 컨센서스 기준 3.2배 — 정점 이익에 낮은 배수),
초과수익은 정상화 지점이 40%보다 높을 때만 나온다. Bear −57% · Base +13% ·
Bull +111%.

교차검증 — 2025 EBITDA 905,276억이 WISEreport 표기값과 **자릿수까지 일치**
(부문 주석 → 연결 재구성의 독립 경로 검증). 컨센서스 2026 EBITDA 443.2조 대
본 모델 379.0조(−15%) — 차이는 하반기 가속 폭이며 두 분기 안에 판정된다.

**엔진 결함 0건.** 다섯 종목 만에 처음이다 — hist 마스크 패턴과 이전 아홉 건의
수정이 새 경로(조정 노드·부문 음수·조원 단위)를 전부 받아냈다. G1 75셀 · G7
400셀 오차 0.

### Phase 9 결과 — 컨센서스 대조와 네 번째 종목 (LGD)

**컨센서스 상세 대조.** 증권사 리포트 원문은 이그레스가 막혀 있지만 WISEreport
스냅샷의 컨센서스 상세(목표주가·투자의견·기관수·직접 추정값)가 열려 있었다.
컨센서스 카드에 **목표주가 행**을 추가했다 — "시장(주가)과 다르다"와
"애널리스트들과 다르다"는 별개의 정보다.

| | 목표주가 컨센서스 | 본 모델 | 관계 |
|---|---|---|---|
| 삼성전기 | 2,350,500원 +70% (20곳) | −27% | **방향이 다름** |
| 티엘비 | 58,796원 +42% (8곳) | +21% | 같은 방향, 모델이 보수적 |
| KT&G | 최근 3개월 제시 없음 | −31% | 커버리지 공백 |
| LGD | 최근 3개월 제시 없음 | +49% | 커버리지 공백 |

컨센서스 EBITDA를 역산값에서 직접값으로 바꿨다(역산과 1.7~1.8% 차이 — 역산
검증). 티엘비의 단가 급등이 **BVH 공법**이라는 것, 베트남 공장, CXL·항공방산
옵션을 애널리스트 코멘트에서 확인해 반영했다. EPS·순이익 레이어가 없다는
구조적 공백은 `ic_memo_framework.md` §5.5에 기록했다.

#### 네 번째 종목 — LG디스플레이 (034220), 적자 사이클

앞의 셋은 전 연도 흑자라 **음수가 엔진을 지나간 적이 없다**. LGD는 2022~2024
3년 연속 영업적자(누적 −5.2조) 후 흑자 전환, 순차입금이 시가총액의 2.3배인
레버리지 구조다. 매출은 제품별로 분해하지 않았다 — 공시 제품 구분이 연도마다
바뀌고(Auto는 2024년 분리) 2022년 제품합이 파생 조정만큼 연결과 어긋난다.

결과: 2030 적정 시가총액 71,483억(+49%). 목표배수 3.5배는 현재 시장 배수
(3.27배, 본 모델 3.26배와 0.3% 차이로 교차검증)와 거의 같다 — 상방의 원천이
배수가 아니라 감가상각 감소(CAPEX가 상각의 0.31배)와 레버리지다. 대만 피어
(AUO 10.7배 +104% · 이노룩스 11.4배 +268%)와 달리 1년 −18.9%로 소외 상태.

**엔진 결함 3건 — 전부 음수 경로.**

1. §3 차트의 선(영업이익률) 스케일이 0~max를 전제해 **음수 좌표가 차트 밖**
   (y=360, 높이 200)으로 그려졌다. min~max 스케일 + 0선(점선)으로 고쳤다.
2. §4가 "자식 2개 이상 + 합계 아님"을 곱셈 분해로 착각해, 실적 오버라이드+
   성장률 자식을 가진 LGD에서 **가짜 매출 분해**(막대=실적, 선=성장률, YoY
   −100%)를 그렸다. `isProductOfChildren`으로 정확히 판정하고, 합계도 곱셈도
   아니면 섹션을 건너뛴다.
3. YoY가 음수 기저에서 부호가 뒤집힌 값을 냈다(−2.5조/−2.1조 → "+20%").
   기저가 0 이하면 `—`로 처리.

네 종목 전 게이트 통과 (G7 삼성전기 420 · KT&G 342 · 티엘비 180 · LGD 160셀).

### Phase 8 결과 — 티엘비 (356860), 진짜 Q×P

세 번째 종목. 앞의 둘과 결정적으로 다른 점은 **공시가 단가와 생산능력을 준다**는
것이다. 삼성전기·KT&G에서는 "공시가 물량을 주지 않으므로 실적 구간을 Q×P로
쪼개지 않는다"가 규약이었는데, 티엘비는 주요제품 가격변동 추이(원/㎡)와
월간 생산능력(㎡)을 5개년 공시한다.

```
매출 = 출하량 × 단가          ← 곱셈 노드
  출하량 = 생산능력 × 가동률   ← 공급 바인딩
```

출하량 = 매출 ÷ 단가로 정의상 정확히 유도되고(G1이 45셀 대사), 생산능력이
물량의 상한이 된다 — modeling_framework의 "공급 바인딩" 케이스가 처음 실행됐다.

| | 삼성전기 | KT&G | 티엘비 |
|---|---|---|---|
| 사업 | 증설 사이클 | 성숙 현금창출 | 메모리 사이클 |
| 밸류에이션 | EV/EBITDA | DCF | EV/EBITDA |
| 매출 분해 | 3부문 합 | 4부문 합 | **출하량 × 단가** |
| 바인딩 | 수요 | 외생 | **공급** |
| 괴리 | −27% | −31% | **+21%** |

**핵심 발견 — 2025년 매출 +43.6%는 사실상 전부 단가였다.**
출하량 213.5 → 235.9천㎡(+10.5%), 단가 8.43 → 10.96억원/천㎡(+29.9%).
생산능력은 2022년 이후 348천㎡로 4년째 고정이고 가동률은 86.1%(2021) →
67.8%(2025)다. 즉 **물량 회복분 +27%가 증설 없이 남아 있다**. 이것이 세 종목 중
유일하게 상방(+21%)이 나온 이유다.

**엔진 결함 세 건을 또 잡았다.** 종목을 하나 더 태울 때마다 나온다.

1. 리포트 §3이 `total_revenue`라는 **노드 이름을 전제**해, 노드명이 `revenue`인
   모델에서 매출 대신 루트(적정 시가총액)를 차트로 그렸다. `revenueId()`가
   관례 이름을 순서대로 찾고 `META.revenueNode`로 명시할 수도 있게 했다.
2. §4의 100% 스택이 **곱셈 분해에 그대로 적용**돼 단위가 다른 출하량과 단가를
   100%로 쌓았다. `isSumOfChildren`으로 갈라 곱셈 모델은 "막대=물량, 선=단가"로
   그린다.
3. 차트의 선 축 라벨이 비율을 전제해 단가 계열에 `2000%`를 찍었다.
   `linePct` 옵션으로 갈랐다.

**Q×P의 정밀도.** G1의 허용오차는 1e-9인데 가동률을 8자리로 반올림했더니
출하량 × 단가가 공시 매출을 재현하지 못했다. 가동률을 배정밀도 그대로 넣어
재현 오차 2.3e-13으로 통과시켰다 — 유도값을 입력으로 되돌릴 때는 자리수가
게이트를 통과할 만큼 남아야 한다.

### Phase 7 결과 — 가정 재보정 (2026-08-28)

2026 상반기 확정 실적을 반영해 Base 가정을 **한 번에, 기록을 남기고** 상향했다.
Phase 5에서 "실적이 앞선다고 그때마다 가정을 올리면 모델이 주가를 뒤쫓는다"며
유보했던 결정이다. 조금씩 따라가지 않고 한 번에 재보정하는 대신, 무엇을 왜
바꿨는지를 `MEMO.revision`으로 모델 안에 남겼다 — 심사 결론 뷰와 리포트 §9에 뜬다.

**매출은 두 방법으로 역산해 교차검증했다.** 계절성(H2/H1 평균)과 YoY 연장이
FY2026을 1% 안에서 일치시켰다.

| 부문 | 상반기 YoY | 계절성법 | YoY법 | 채택 | 기존 → 신규 |
|---|---|---|---|---|---|
| 컴포넌트 | +22.5% | 63,079 | 63,663 | 63,371 | +12.3% → **+21.9%** |
| 패키지 | +40.7% | 32,612 | 32,378 | 32,495 | +15.4% → **+41.2%** |
| 광학 | +7.6% | 39,671 | 41,047 | 40,359 | +3.0% → +5.8% |

**불리한 방향도 그대로 반영했다.** 패키지 감가상각 증가율은 가정 +12%보다 나쁜
+17.7%였고, 광학은 −5%를 가정했는데 +8.1%였다. 유리한 것만 골라 올리면 재보정이
아니라 합리화가 된다.

**목표배수 15배 → 18배.** 2026 부문 EBITDA 비중으로 해외 피어 배수를 가중하면
22.9배(MLCC 24.0 × 55.2% + FC-BGA 26.7 × 32.2% + 광학 8.3 × 12.7%)다. 다만 그
배수들은 지금 시점 TTM이고 목표배수는 2030년에 적용하는 값이라, 테마 프리미엄의
5년 존속은 가정하지 않고 국내 AI 기판 2026E 상단(21.3배) 아래로 잡았다.

**결과**

| | 재보정 전 | 재보정 후 |
|---|---|---|
| 2026 EBITDA | 20,165억 | 25,220억 |
| 2030 EBITDA | 30,422억 | 41,406억 |
| 2030 적정 시가총액 | 461,430억 | **750,401억** |
| 현재가 대비 괴리 | −55% | **−27%** |
| 컨센서스와의 격차 | +48% | **+18%** |
| 현재가 함의 배수 (2030) | 34배 | 24.9배 |
| 현재가 정당화에 필요한 EBITDA | 모델의 2.26배 | 모델의 **1.38배** |

시나리오도 함께 재조정했다 — Base가 오르면 Bull/Bear를 두는 순간 순서가 뒤집힌다.
Bear −79% · Base −27% · Bull +44%로, **현재 가격은 Base와 Bull 사이에 있다.**

**게이트가 한 번 잡았다.** 개정 메모를 `desc` 맨 앞에 붙이면서 `[주관]` 태그를
밀어냈고 G10이 12개 노드를 전부 실패시켰다. 태그가 먼저, 이력은 뒤로 고쳤다.

### Phase 6 결과

**어투.** 서술형을 전부 걷어내고 개조식으로 바꿨다. 종결은 "~함/~임/명사형",
결론은 명사구로 앞세운다 — "투자 판단은 패키지솔루션 하나에 달려 있다"가
"투자판단의 Key는 **패키지솔루션**"이 된다. 두 종목의 MEMO와 엔진의 화면 문구를
전부 다시 썼다. 규약은 `framework/ic_memo_framework.md` §4.2b.

**강조.** 본문에 `**굵게**`를 도입했다. 이스케이프한 뒤에 변환하므로 데이터가
태그를 주입할 수 없다. 마크다운을 흉내내지 않은 이유는 심사 자료에서 필요한
강조가 "이 숫자가 결론이다" 하나뿐이기 때문이다. 한 항목에 하나만 쓴다 —
둘 이상 굵게 하면 아무것도 강조되지 않는다.

**차트.** 표는 값을, 차트는 추세를 준다. 외부 라이브러리 없이 인라인 SVG로
세 종류를 그린다(외부 요청 0건 규약).

| 함수 | 쓰이는 곳 |
|---|---|
| `icSvgBars` | 매출 추이(추정 구간 음영 + OPM 선), 분기 매출·영업이익률 |
| `icSvgHBars` | 피어 EV/EBITDA 순위, 시나리오 괴리, 민감도 영향도 |
| `icSvgStack` | 부문 구성비 100% 스택 |

**타이포 위계.** 섹션 제목 17px → 카드 제목 13.5px → 본문 12.5px → 근거 11.5px로
네 단을 벌렸다. 색도 함께 낮춰(#0F0F12 → #4B5563 → #6B7280) 훑을 때 층이 보인다.

**리포트 §4의 잡음을 걷었다.** 부문 아래 자식이 대부분 가정변수(성장률·실적
오버라이드)라 0.0% 행만 늘어나고 있었다. 금액 단위의 계산 노드만 펴고,
비용 트리를 따로 붙여 마진 이야기를 할 수 있게 했다.

### Phase 5 결과

개선 제안 6건을 전부 실행했다.

**분기 축.** DART 정기보고서 영업부문 주석을 파서로 붙였다(`dart_fetch segments`).
정기보고서는 연초 누적을 주므로 분기값은 누적의 차분이다. 2024Q1~2026Q2 10개 분기의
분기합이 연간 확정값과 정확히 일치한다(2024 102,941/7,350 · 2025 113,145/9,133) —
파싱의 교차검증이다. `build_model.py`가 `quarterly.json`을 `QUARTERLY`로 자동 주입한다.

**모니터링 뷰.** 당해 누적 실적 대 모델 연간 추정의 진행률, 분기 확정 실적표,
아이디어별 확인지표. `ideas[].track`에 `{seg, metric, good}`을 적으면 화면이
`QUARTERLY`에서 계산한다. 기타비용률은 매출 − 영업이익 − 감가상각비로 유도해
모델의 비용 분해와 정의를 맞췄다.

**자동 민감도.** 전 입력을 ±10% 흔들어 정렬. 결과는 비용률이 성장률을 압도한다 —
컴포넌트 기타비용률 ±20.4%, 목표배수 ∓9.9%, 물량 성장률 ±1.7%.

**컨센서스 블록 · 게이트 G11 · 숫자 인용.** MEMO 산문에 `{{node@연도}}`를 쓰면
화면이 모델값으로 치환한다. G11이 인용 해석과 구조 완전성을 검사한다.

**문체.** 서술형 문단을 걷어내고 2단 항목(▪ 주제 / – 근거)으로 바꿨다.
스키마는 `framework/ic_memo_framework.md` §4.2.

#### 2026 상반기 확정이 말한 것

매출 66,663억 · 영업이익 7,209억 · EBITDA 11,806억. 본 모델 2026 연간 추정 대비
진행률 54%·65%·59%로 선형(50%)을 앞서고, 컨센서스 전제 29,752억 기준으로는 40%다.
**실적이 모델 위, 컨센서스 아래에 있다.** 패키지 부문 영업이익률 4.4%(2025Q2) →
13.6%, 컴포넌트 기타비용률 79.3% → 76.5%. 아이디어 1·2가 지금까지는 맞는 쪽이다.

Base 가정은 일부러 손대지 않았다. 실적이 앞선다고 그때마다 가정을 올리면 모델이
주가를 뒤쫓게 되고 "무엇을 전제했는가"의 기록이 사라진다.

#### 해외 피어 — 재평가는 국내 현상이 아니었다

`stockanalysis.com`이 열려 다섯 종목을 받았다. EV/EBITDA(TTM) 기준으로
이비덴 39.1 · 야게오 24.5 · 무라타 23.5 · 신코덴키 14.3 · TDK 11.3배,
1년 수익률은 이비덴 +450% · 야게오 +337% · 무라타 +205%다.

**AI 기판·MLCC의 글로벌 재평가**이지 한국 시장의 과열이 아니다. 더 중요한 것은
선행 지표다 — PER 기준 삼성전기 2026E 69.9배 대 이비덴 Forward 62.1배로,
국내 피어와 비교할 때 보이던 2~3배 프리미엄이 글로벌 FC-BGA 선발주자와
비교하면 12% 차이까지 좁혀진다. 목표배수 15배가 낮게 잡혔을 가능성이 이 데이터로
처음 제기됐다.

출처가 국내(WISEreport)와 해외(stockanalysis)로 갈리므로 국내끼리·해외끼리만
나란히 놓는다. 시가총액은 현지 통화 그대로 둔다 — 환산하면 환율 기준일이
하나 더 늘고 대사할 것도 하나 더 늘어난다.

**게이트를 하나 메웠다.** 편집 중 `PEERS` 블록이 통째로 사라졌는데도 전 게이트가
통과했다. 선택 블록이라도 "있다가 없어진 것"은 잡아야 한다 — `MEMO`를 쓰는
모델에 `PEERS`가 없으면 G11이 실패한다.

#### 두 번째 종목 — KT&G (033780)

엔진이 종목에 종속되지 않는지 확인하려고 성격이 반대인 종목을 태웠다.

| | 삼성전기 | KT&G |
|---|---|---|
| 사업 | 증설 사이클 | 성숙 현금창출 |
| 밸류에이션 | EV/EBITDA 배수법 | DCF |
| 부문 | 3개 | 4개 |
| 엔진 경로 | — | PREV 자기참조 2종(할인계수 체인·현재가치 누적) |

**엔진 결함 세 건을 이 과정에서 잡았다.**

1. `UNITS`의 선택 필드(`excelOneDecimal` 등)가 없으면 Excel 빌드가 죽었다.
   초기화 블록으로 몰면 이름 없는 구문이라 하네스가 떼어가지 못해 브라우저에서만
   되고 G7에서 죽는다 — 쓰는 자리마다 기본값을 세우는 쪽으로 고쳤다.
2. 밸류에이션 뷰가 `ebitda` 노드를 전제해 DCF 모델에서 화면이 통째로 비었다.
3. 표지의 "현재가 함의 배수"가 DCF에서 `—`로 떠 화면이 고장난 것처럼 읽혔다.

결과: 2030 적정 시가총액 124,303억, 현재 시가총액 181,149억 대비 −31%.
교차검증 — 2025 EBITDA 16,480억 기준 EV/EBITDA 11.51배 대 WISEreport 11.42배(0.8% 차이).
전 11개 게이트 통과(G7 333셀 · G1 44셀).

### Phase 4 결과

모델은 "가정을 바꾸면 얼마"에 답했지만 "무엇을 사는 것인가"에는 답하지 못했다.
정성 레이어와 리포트 조판을 얹어 그 공백을 메웠다.

- **투자 아이디어 뷰** — `MEMO.ideas`. 아이디어마다 논거·촉매·확인지표·리스크,
  그리고 **반증 조건**을 함께 적는다. 반증 조건이 없으면 분기 리뷰가
  "여전히 유효하다"는 말만 반복하게 된다.
- **심사 쟁점** — `MEMO.debate`. 한 질문에 찬반을 같은 무게로 적는다.
- **심사 리포트 뷰** — 10개 섹션을 한 장으로 조판. `@media print`가 붙어
  인쇄·PDF로 그대로 나간다 (A4 8쪽). 본문의 숫자는 전부 `MODEL`에서 그 자리에서
  다시 읽으므로 **리포트와 모델이 어긋날 수 없다** — 숫자를 손으로 적지 않는 것이
  이 뷰의 유일한 규칙이다.
- **모바일 결함 수정** — 폭 1100px 이하에서 사이드바가 화면 밖으로 밀려나는데
  그것을 되돌릴 ☰ 버튼에 인라인 `display:none`이 박혀 있었고 걷어내는 코드가
  없었다. 좁은 화면에서는 투자심사 그룹 전체에 도달할 방법이 없었다. 서랍·스크림·
  Esc로 고쳤다.

**해외 피어는 Phase 5에서 채웠다.** 이그레스 정책에 `stockanalysis.com`이
추가되면서 무라타·TDK·야게오·이비덴·신코덴키를 받았다
(`tools/peer_fetch.py --foreign`). 그전까지는 우회하지 않고 비워 두고
`PEERS.missing`으로 공백을 화면에 드러냈다.

### Phase 3 결과 (2차)

- `tools/build_excel.py` — 8시트 Excel. 수식 변환은 JS `astToExcel`이 정본이고
  파이썬은 배치만 한다. 변환기가 둘이 되면 G7이 자기 자신을 검사하게 된다.
- **게이트 G7 통과** — 42노드 × 10년 = 420셀, `xlsx_eval.py`가 셀 수식을
  참조를 따라가며 재계산해 대사. 오차 0. **이제 건너뛰는 게이트가 없다.**
- CDN 스크립트 3종과 웹폰트 2종 제거 — `model.html`이 **외부 요청 0건**으로 동작
- `.claude/skills/ic-model/SKILL.md` — 종목명을 주면 이 파이프라인이 돈다

### Phase 3 결과

밸류에이션을 문서가 아니라 **모델 노드**로 만들었다. 루트가 적정 시가총액이고,
목표배수가 슬라이더라 심사장에서 "배수를 10배로 보면 얼마인가"에 즉답할 수 있다.

```
적정 시가총액 = EBITDA × 목표 EV/EBITDA − 순차입금
```

- 심사 뷰 4종 — 투자 개요 · 밸류에이션 · 시나리오 · 심사 결론 (`MARKET` 선언 시에만 표시)
- 시나리오 프리셋 — `SCENARIOS` 선언을 케이스로 자동 등록. Base는 초안값 그 자체
- 판단 텍스트는 `MEMO`로 분리 — 모델 수치와 섞지 않는다

**핵심 발견**: 현재 시가총액이 2025년 EBITDA의 58배, 본 모델 2030년 추정으로도
34배다. Base(15배)는 −55%를 가리키지만 이를 "고평가"로 단정하지 않았다 —
주가가 1년에 8.8배 오른 종목에서 실적 모델과 가격이 벌어지는 것은 정상이고,
간극은 모델이 담지 못한 서사에 있다. 그래서 역산을 함께 둔다: 현재 가격을
정당화하려면 2030 EBITDA가 모델 추정의 **2.3배**여야 한다. 심사의 질문은
"적정가가 얼마인가"가 아니라 "그 2.3배가 어디서 오는가"다.

현재 주가는 Bull 시나리오(+16%)와 거의 같은 자리에 있다.

### Phase 3 결과 (3차) — 피어 그룹

목표배수는 결과를 가장 크게 좌우하는 가정인데 근거가 기억("역사적 5~8배")에
있었다. `tools/peer_fetch.py`로 같은 출처·같은 기준일의 피어 배수를 받아
근거를 데이터로 바꿨다. `PEERS` 블록으로 data.js에 들어가고 밸류에이션 뷰에 뜬다.

**핵심 발견 세 가지**

1. 기판 3사도 실적 기준 35~55배다 — 삼성전기만의 현상이 아니라 AI·기판 테마
   전반의 재평가다.
2. 그래도 2026 컨센서스 기준으로는 삼성전기 34.6배 vs 피어 14.1~21.3배로
   프리미엄이 실재한다. 성숙 부품사(LG이노텍 8.3, 비에이치 4.8)는 여전히 한 자릿수다.
3. **컨센서스가 본 모델보다 48% 높은 2026 EBITDA를 전제한다**(29,752억 vs 20,165억).
   "2030년에 2.3배가 필요하다"는 갭의 상당 부분을 시장은 이미 2026년에 반영했다.
   갭은 시장의 비합리가 아니라 본 모델 가정이 보수적인 데서도 온다.

목표배수 15배는 AI 기판 피어의 2026E 범위(14.1~21.3배) 하단에 놓인다.

**교차검증**: 삼성전기 2025 EV/EBITDA가 본 모델 57.9배 vs WISEreport 56.8배로
2% 차이다. 서로 독립적인 경로에서 나온 값이라 밸류에이션 산식의 검산이 된다.

**빠졌던 것을 채웠다**: 계획서는 심사 뷰 6종을 적었는데 3종으로 합쳐 냈고,
그중 시나리오는 화면이 아예 없었다(문서에만 있었다). Bull/Base/Bear를 나란히
놓고 보는 것이 심사에서 가장 자주 하는 일이라 전용 뷰로 만들었다.

### Phase 2 결과

루트를 총매출에서 **영업이익**으로 올렸다. 부문 비용을 감가상각비와 기타비용으로
나눈 것이 이 레이어의 전부다 — 공시가 부문별 원가 명세를 주지 않아 그 이상 쪼개면
근거가 없다.

- 감가상각비는 전년 대비 증가율로 굴린다 (매출에 연동하지 않는다 — 이미 집행된 투자)
- 기타비용은 매출 × 비율로 굴린다 (원재료·인건비가 물량에 연동)
- 부문 영업이익 합 = 연결 영업이익, 전 5개년 오차 0. G1이 60셀 대사.

**핵심 발견**: 패키지솔루션 매출이 5년간 37% 느는 동안 이 부문 감가상각비가
1,119 → 3,727억으로 3.3배 늘어 영업이익이 4,646억(2022)에서 1,352억(2025)으로
줄었다. 이익 회복 스토리의 상당 부분이 "증설이 끝나가는 것"에 달려 있다 —
기타비용률을 2025년 수준으로 고정해도 2030 영업이익률이 8.1%에서 10.5%로 오른다.
비용률 개선이 얹는 몫은 2030년 기준 2,605억(13%)이다.

### Phase 0에서 고친 것

| 등급 | 내용 |
|---|---|
| P0 | `PREV(자기노드)` 누적 지원 — 연도축 순차 평가로 전환. 감가상각 롤포워드·누적 잔액이 가능해졌다 |
| P0 | 순환참조 검출 복원 — 고리에 걸린 전 노드에 표시 |
| P0 | `DEFAULTS_S`를 `MODEL`에서 파생 — 중복 데이터 제거 |
| P2 | 구성비 분모 — 뺄셈 루트에서 자식 절대값 합을 쓰도록 (Phase 2에서 추가) |
| P1 | 단위 비종속화 — `UNITS` 한 곳에서 통화 표기 결정 (억원 적용) |
| P1 | 레거시 노드 id 하드코딩 제거 |
| P1 | 종목 정체성을 `META`로 분리 — 제목·브랜드·저장키 |

### 남은 결함

전부 해결됐다.

| 등급 | 내용 | 해결 |
|---|---|---|
| P1 | CDN 3종 의존 | 스크립트·웹폰트 전부 제거. `model.html`이 외부 요청 0건으로 동작 |
| P1 | Excel 생성 경로 이중화 | `tools/build_excel.py`를 정본으로. 수식 변환은 JS `astToExcel`이 정본이고 파이썬은 배치만 |
| P2 | Chart.js 미사용 로드 | 제거 |
| P2 | 구성비 분모 | 뺄셈 루트에서 자식 절대값 합 사용 |
