# 📌 실행 지시문 (새 대화에 이 파일 + equity_report_skill.zip 을 함께 첨부하고, 아래를 복사해 첫 메시지로 보내세요)

════════════════════════════════════════════════════════
아래 한 덩어리를 그대로 복사해서 붙여넣으면 됩니다 (종목명만 바꾸세요)
════════════════════════════════════════════════════════

첨부한 `equity_report_skill.zip`(또는 `종목분석리포트_생성스킬.md`)은 종목 심층분석 리포트를
만드는 스킬 지침이야. 이 지침을 **처음부터 끝까지 그대로 따라서** 리포트를 만들어줘.

종목: **○○○**   ← 여기에 종목명만 바꿔 넣으세요 (예: 삼성전자 / 티엘비 / 로킷헬스케어)

반드시 지킬 것:
1. 먼저 zip 압축을 풀어 SKILL.md·report_template.css·build_pdf.py·gen_candles.py 를 확인해.
2. web_search로 그 종목을 6~12회 이상 조사해 (현재가·시총·지분율·재무·사업부문·경쟁사·향후이벤트).
   특히 무상증자/유상증자/권리락이 있으면 주식수·주가 기준을 정확히 확인해.
3. report_template.css 를 <style>로 넣고, SKILL.md의 "표준 페이지 구성"대로 HTML을 작성해.
   - 표지: 서술형 회사소개 → 회사개요/주주 2박스 → **일봉 캔들 주가차트(gen_candles.py 사용)**
     → 재무표 → 최근분기 심층해석 3줄
   - 2p: 요약·Thesis·투자아이디어(3~4개) 통합
   - 이후: 사업부문 심층 → 타임라인 → (해자 또는 경쟁) → 밸류 → 이벤트캘린더 → 결론 → Appendix
   - 업종에 맞게 가감(제조·부품=경쟁/증설 중심, 바이오=파이프라인/해자 중심)
4. 문체는 **개조식(~음/함/임)**, 단 면책조항만 존댓말. 본문에 **한자 0개**(한글/영어로).
   색상은 CSS의 검붉은색 팔레트 그대로.
5. build_pdf.py 로 PDF를 생성하고, pdftotext로 페이지 넘침·한자를 검수한 뒤,
   /mnt/user-data/outputs/ 에 저장하고 present_files로 나에게 줘.

⚠️ 이 대화에 **"코드 실행 및 파일 생성" 기능이 켜져 있어야** PDF가 나와.
   (설정에서 켜거나, 켜진 대화에서 진행할 것. 안 켜져 있으면 Claude가 알려줄 거야.)

════════════════════════════════════════════════════════

## 참고: 잘 안 될 때 체크리스트
- PDF가 안 나오고 코드/텍스트만 나온다 → 코드 실행 기능이 꺼진 것. 설정에서 켜세요.
- 페이지가 1장만 나온다 → "build_pdf.py를 한 번 더 실행해줘"라고 하면 됩니다(간헐적 렌더 이슈).
- 주가차트가 밋밋한 선이다 → "주가차트를 gen_candles.py로 일봉 캔들로 그려줘"
- 한자가 보인다 → "본문 한자를 모두 한글/영어로 바꿔줘"
- 어체가 '~하다'다 → "본문을 개조식(~음/함/임)으로 바꿔줘. 면책만 존댓말로."

## 참고: 부분만 요청해도 됨
전체 리포트가 아니라 특정 페이지·요소만 원하면 그렇게 말해도 됩니다.
예) "티엘비 표지 1페이지만 만들어줘" / "○○ 종목 이벤트 캘린더만" 등.


============================================================
# 이하 스킬 본문 (SKILL.md)
============================================================

---
name: equity-deep-dive-report
description: 사용자가 종목명(또는 티커)만 제시하면, 해당 상장기업에 대한 "방배동 베어그릴스 RESEARCH" 스타일의 멀티페이지 심층 애널리스트 리포트를 검붉은색 디자인의 PDF로 생성한다. "○○○ 리포트 만들어줘", "○○○ 분석해줘", "○○○ 보고서" 등 종목 분석/리포트 요청 시 사용. 국내외 상장사 모두 대상.
---

# Equity Deep-Dive Report Generator (심층 종목분석 리포트 생성기)

> **▶ 이 스킬을 처음 보는 Claude에게:** 사용자가 종목명을 주면, 이 문서의 STEP 1~5를 순서대로
> 실행해 A4 멀티페이지 PDF 리포트를 만들어 `present_files`로 전달하라. 코드 실행/파일 생성
> 기능이 필요하다(HTML→Chromium→PDF). 같은 폴더의 `report_template.css`(스타일),
> `build_pdf.py`(PDF 변환), `gen_candles.py`(일봉 캔들)를 사용한다. 기능이 꺼져 있으면
> 사용자에게 "코드 실행 및 파일 생성"을 켜달라고 먼저 안내하라.


## 0. 이 스킬의 목적
사용자가 **종목명 하나만** 말하면, 아래 표준 구조·디자인·문체를 그대로 재현한
**A4 멀티페이지 애널리스트 리포트 PDF**를 자동 생성한다.
결과물은 국내 증권사 리포트급의 밀도와 완성도를 목표로 한다.
바이오·플랫폼뿐 아니라 **제조·부품·반도체·소비재 등 전 업종**에 대응하며,
종목 성격에 맞춰 페이지 구성을 유연하게 가감한다(섹션 STEP 2·4 참조).

리포트 브랜드명(고정): **방배동 베어그릴스 RESEARCH** / 태그: COMPANY DEEP-DIVE

---

## 1. 실행 순서 (반드시 이 순서대로)

### STEP 1 — 리서치 (web_search 다수 호출)
종목명을 받으면 **먼저 충분히 검색**한다. 최소 6~12회, 다음 주제를 각각 검색:
1. `<종목> 기업개요 사업영역` — 무슨 회사인지, 사업 부문
2. `<종목> 최근 실적 매출 영업이익 <올해>` — 연간·최근분기 재무
3. `<종목> 주가 시가총액 <현재연도>` — 현재가·52주밴드·시총
4. `<종목> 주주현황 최대주주 지분율 발행주식수` — 지배구조
5. `<종목> 부채비율 재무구조 유상증자 전환사채` — 재무건전성·오버행
6. 각 **사업부문별**로 개별 검색 (부문이 여러 개면 각각): `<종목> <사업부문> 진행상황 임상/수주/상업화`
7. `<종목> 경쟁사 비교 시장점유율` — 경쟁 구도
8. `<종목> 향후 일정 이벤트 신제품 계약 임상 <다음연도>` — 촉매
9. 필요 시 `<종목> 기술 특허 차별성 해자` — 기술적 해자

**원칙**: 각 사업부문·경쟁사는 개별 쿼리로 검색(합치면 얕아짐). 현재가·시총·지분율 등
변동 수치는 반드시 검색으로 최신화. 확신이 있어도 present-day 사실은 검색.

### STEP 2 — 데이터 정리
검색 결과를 다음 버킷으로 정리한다:
- 회사 개요(설립·상장·대표·주요사업·자회사)
- 주주현황(최대주주·지분율·발행주식수·외국인·보호예수)·재무지표(부채구조/밸류에이션 PER 등)
- 주가 궤적(상장/공모가 → 주요 고점·저점 → 현재가). **일봉 캔들용 변곡점(일자, 가격)을 5~20개** 확보
- 재무(최근 3~4개 기간 매출·성장률·영업손익·순손익·OPM)
- 사업부문별 진행상황 + 타임라인/Appendix용 마일스톤(일자별)
- 경쟁사(피어 비교표·밸류에이션) / 기술적 해자 요소
- 향후 이벤트(상승 촉매 / 하락 위험)

**주식수 변동 이벤트 주의(중요)**: 무상증자·유상증자·액면분할·권리락이 있으면 주가 레벨과 주식수가 크게 바뀐다.
- **수정주가(소급반영)와 실제 거래가를 혼동하지 말 것.** 차트·표에 어느 기준인지 반드시 명시.
- 무상증자 1:1 = 주식수 2배, 유증 X% = 추가 희석. 발행주식수 변화(예: 983만→2,376만주)와 EPS 희석률을 본문에 적시.
- 밸류 비교는 **절대 주가가 아닌 시가총액·EPS(PER) 기준**으로. (티엘비 사례: 권리락으로 주식수 2.42배 → 절대주가만 보면 오판)

**업종별 페이지 유연화**: 표준 구성은 바이오/플랫폼 기준이다. 종목 성격에 맞게 페이지를 가감한다.
- **제조업·부품·반도체**: '기술적 해자' 페이지는 축소하고 **경쟁 포지셔닝(피어 PER 비교)·증설/CAPA·전방산업 사이클**을 비중 있게. 파이프라인 대신 **제품 믹스(P)×생산능력(Q)** 프레임.
- **바이오·신약·플랫폼**: 파이프라인 타임라인·기술 검증(임상 단계·통계 유의성)·기술적 해자를 강조.
- **내수·소비재·서비스**: 브랜드·점유율·채널·마진 구조 중심.
- 사업부문이 1~2개인 순수 플레이면 사업 페이지를 2p로 축약, 다각화 기업이면 늘린다.

### STEP 3 — HTML 작성
`report_template.css`(이 스킬 폴더 내)를 `<style>`로 감싸 `<head>`에 넣고,
아래 **표준 페이지 구성**대로 `<body>`를 채운다. (섹션 4·5 참조)
주가 일봉 캔들은 `gen_candles.py`(섹션 5-3)로 SVG를 생성해 삽입한다.

### STEP 4 — PDF 변환
```bash
python3 <skill_dir>/build_pdf.py report.html /mnt/user-data/outputs/<종목>_심층분석리포트.pdf
```

### STEP 5 — 검수
- `pdftotext -layout`로 텍스트를 추출해 **페이지별 마지막 줄**을 확인(넘침/누락 점검).
- 페이지 헤더 번호(01~) 연속성 확인. 본문 **한자 0개** 확인. 개조식(~음/함/임) 확인.
- 캔들차트는 픽셀 분석으로 상승/하락 캔들이 모두 렌더링됐는지 확인(섹션 5-3 하단).
- 간헐적으로 Chromium 렌더가 1페이지만 출력될 수 있음 → 페이지 수가 예상과 다르면 **build_pdf.py를 한 번 더 실행**.
- 마지막에 `present_files`로 PDF 전달 + 3~5줄 핵심 요약.

---


## 2. 디자인 규칙 (절대 준수)

- **색상**: 검붉은색 팔레트. CSS 변수는 `report_template.css`에 정의됨
  (`--navy:#5c1418`, `--navy2:#7d1f24`, `--accent:#a8323a`, `--red:#8f1d18`,
  긍정은 `--green:#1e7a48` 유지). **색을 바꾸려면 이 변수만 수정**하면 전체 반영.
- **폰트**: `'Noto Sans CJK KR'` (컨테이너에 설치돼 있음). 본문 9.3px.
- **레이아웃**: A4, 각 페이지는 `<div class="page">`로 감싸고 자동 페이지 브레이크.
- **여백/밀도**: 국내 리포트처럼 촘촘하게. 표·콜아웃·스코어카드·타임라인 적극 활용.
- **렌더링은 Chromium(Playwright)** 사용 — flex/grid/SVG 완벽 지원. `build_pdf.py`가 처리.

---

## 3. 문체 규칙 (절대 준수)

- **본문은 개조식(음슴체)**: 평서형 종결어미를 모두 `~음/~함/~임`으로.
  - 예: 있다→**있음**, 한다→**함**, 이다→**임**, 크다→**큼**, 만든다→**만듦**,
    견인한다→**견인함**, 나온다→**나옴**, 되다→**됨**.
  - HTML을 다 쓴 뒤 아래 파이썬으로 **일괄 변환**하면 안전(문장 끝만 치환):
    ```python
    import re
    html = open('report.html').read()
    m = {'있다':'있음','없다':'없음','한다':'함','이다':'임','아니다':'아님','크다':'큼',
         '작다':'작음','다르다':'다름','나온다':'나옴','만든다':'만듦','낳는다':'낳음',
         '갖는다':'가짐','가깝다':'가까움','어렵다':'어려움','된다':'됨','벌어진다':'벌어짐',
         '것이다':'것임','방식이다':'방식임','구조다':'구조임','기업이다':'기업임',
         '핵심이다':'핵심임','오독이다':'오독임','축이다':'축임','점이다':'점임',
         '요인이다':'요인임','신호다':'신호임','성장주다':'성장주임','단계다':'단계임',
         '초기다':'초기임','밸류다':'밸류임','영역이다':'영역임','이벤트다':'이벤트임',
         '해부한다':'해부함','제시한다':'제시함','정리한다':'정리함','구분한다':'구분함',
         '견인한다':'견인함','적용한다':'적용함','기능한다':'기능함','성립한다':'성립함',
         '드러난다':'드러남','배열했다':'배열했음','출원했다':'출원했음','분류됐다':'분류됨',
         '않았다':'않았음','따랐다':'따름','본다':'봄','시험대다':'시험대임','잦다':'잦음'}
    for k in sorted(m, key=len, reverse=True):
        html = re.sub(re.escape(k)+r'\.(?=(<|\s|$|&))', m[k]+'.', html)
    open('report.html','w').write(html)
    ```
- **예외**: 마지막 면책조항·주의문구는 **존댓말 유지**(~습니다/~바랍니다/~하십시오).
  법적 고지의 정중함을 위해 개조식으로 바꾸지 않음.
- **한자 사용 금지**: 본문·표·차트 어디에도 한자를 쓰지 않음. 모두 한글 또는 영어로.
  - 등급/방향성: 高/中/低/最 → **상/중/하/최고**(예: `↑↑ 상`, `↑ 중상`, `잠 최고`), 强 → **강/중강**
  - 국가/시점: 美 → **미국**, 韓·中·印 → **한·중·인도**, 下 → **하반기**, 前 → **전**, 月 → **월**
  - HTML 작성 후 아래로 잔여 한자를 점검(0이어야 함):
    ```python
    h=open('report.html',encoding='utf-8').read()
    rr=[(0x3400,0x4DBF),(0x4E00,0x9FFF),(0xF900,0xFAFF)]
    print({c for c in h if any(lo<=ord(c)<=hi for lo,hi in rr)} or '한자 없음 OK')
    ```
  - 섹션 번호 로마숫자(Ⅰ,Ⅱ…)는 한자가 아니므로 허용.
- 캐치프레이즈 부제는 "○○ — '△△'와 '□□' 사이" 식으로 종목의 핵심 긴장을 한 줄로.

---

## 4. 표준 페이지 구성 (종목 성격에 맞게 가감)

| 페이지 | 헤더 라벨 | 내용 |
|--------|-----------|------|
| 1 (표지) | (마스트헤드) | 브랜드·종목명·티커·부제 → **서술형 회사소개 1문단** → 회사개요/주주현황 2박스 → **주가차트(SVG)** → 핵심재무표 → **최근분기 심층해석 3줄(강조박스)** → 각주·면책 |
| 2 | 요약 · Thesis · 투자 아이디어 | Executive Summary(한 문장) → Bull/Bear 2단 → **핵심 투자 아이디어 3~4개**(카드형: 번호·제목·투자기간·본문·촉매/확인지표/리스크) → 성향별 매핑 종합. **한 페이지로 통합.** |
| 3 | 사업 포트폴리오 조망 | 사업 전체 구조 + '현재가치 vs 잠재가치' 매트릭스(bar) + 스냅샷 표 |
| 4~ | 사업부문 심층 ①,②… | 부문별 진행상황·데이터·촉매·리스크 (부문 수에 따라 페이지 조절) |
| N | 사업부문 진행 타임라인 | **간트차트형 타임라인**(완료=초록/진행=크림슨/미래=회색) + 일자별 마일스톤 표 |
| N+1 | 기술적 해자 (Moat) | 해자 요소 표(강도 등급) + '진짜 해자 vs 착각' 2단 + 비판적 검증 |
| N+2 | 밸류에이션 | 밸류 지표·논쟁점·강세/약세 |
| N+3 | 경쟁 포지셔닝 | 경쟁사 비교표 + 포지셔닝 맵 서술 |
| N+4 | 주가 이벤트 캘린더 | **상승 촉매표 / 하락 위험표**(시점·이벤트·영향↑↓·비고) |
| N+5 | 종합 결론 & 시나리오 | Bull/Base/Bear 시나리오 + 투자의견 박스 + 한줄총평 |
| 마지막 | 요약·출처·면책 | One-Page Recap 표 + 자료출처·방법론 + **면책(존댓말)** |
| 부록 | Appendix. 사업영역별 주요 진행상황 | **사업부문 × 일자별 진행 로그 표**(applog): 왼쪽 사업영역(크림슨 박스), 오른쪽 시점 역순 불릿. 사업 파이프라인이 여러 개인 종목은 필수 |
| Appendix | 사업영역별 주요 진행상황 | **사업영역별 진행상황 로그 표**(좌: 사업영역 / 우: 시점별 "– (시점) 내용" 나열). 맨 뒤 부록. |

> **주의**: 요약·Thesis·투자아이디어는 **2페이지에 통합**한다. 별도의 "투자 판단 요약 스냅샷"
> (rating strip)이나 페이지별 "리포트 구성 안내" 콜아웃은 **넣지 않는다**(중복·군더더기 제거).
> 투자의견·밸류에이션·트리거 요약은 표지 심층해석과 마지막 One-Page Recap 표로 충분.
>
> 페이지 수는 **종목 복잡도에 따라 10~13p 유연 조절**. 사업부문이 1~2개면 축약,
> 다각화 기업이면 부문 페이지를 늘린다. 각 `.page` 헤더 우측 라벨에 `01.`,`02.`… 연번
> (표지·통합요약 페이지는 번호 없이 라벨만).

---

## 5. 핵심 컴포넌트 HTML 스니펫 (복붙용)

### 5-1. 문서 뼈대
```html
<!DOCTYPE html><html lang="ko"><head><meta charset="UTF-8">
<style>
/* ↓↓↓ report_template.css 내용을 통째로 여기에 붙여넣기 ↓↓↓ */
</style></head><body>
  <!-- 페이지들 -->
</body></html>
```

### 5-2. 표지 마스트헤드 + 타이틀 (고정 브랜드)
```html
<div class="page">
  <div class="masthead">
    <div class="brand"><span class="firm">방배동 베어그릴스 RESEARCH</span></div>
    <div class="doctype">
      <span class="tag">COMPANY DEEP-DIVE</span><br>
      대한민국 · <섹터><br>발행일: <YYYY년 M월 D일>
    </div>
  </div>
  <div class="titlebar"><span class="co"><종목명></span><span class="ticker"><티커> · <시장></span></div>
  <div class="subtitle"><한 줄 캐치프레이즈></div>

  <p style="font-size:8.9px; margin:9px 0 4px 0;">동사는 <핵심기술/사업>을 영위하는 <업종> 기업임. ... 1) ... 2) ... 3) ...</p>

  <div class="ov-grid">
    <div class="ov-box"><div class="bt">회사 개요</div>
      <div class="ov-row"><span class="k">설립 / 상장</span><span class="v">...</span></div>
      <!-- 창업자·대표 / 상장방식 / 주요사업 / 주요자회사 -->
    </div>
    <div class="ov-box"><div class="bt">주주 현황 &amp; 재무 지표</div>
      <!-- 최대주주 / 자사주·외국인 / 발행주식수 / 보호예수 / 재무구조 -->
    </div>
  </div>
  <!-- 주가차트(5-3) / 재무표(5-4) / 심층해석(5-5) / 면책 -->
</div>
```

### 5-3. 주가 차트 — 일봉 캔들 (자동 생성)
> 라인차트가 아니라 **일봉 캔들차트**로 그린다. 실제 일별 OHLC 전체는 공개데이터로 구하기
> 어려우므로, **검색으로 확인한 변곡점(일자·가격)을 선형 보간**해 일봉 밀도(약 200~250개)로
> 재현하는 근사 도식을 쓴다. 아래 `gen_candles.py`로 SVG 조각을 만들고 chartbox에 삽입한다.
>
> **주식수 변동 주의**: 무상증자·유증·권리락이 있으면 (a) 실제 거래가 기준으로 그리되 권리락
> 지점에 마커를 찍거나, (b) 수정주가로 통일하되 어느 기준인지 캡션에 명시. 절대 혼용 금지.

```python
# gen_candles.py — 변곡점 보간 일봉 캔들 SVG 생성
import random; random.seed(7)
# (거래일 인덱스, 종가) 변곡점 — 검색으로 확인한 상장/최저/최고/급등·급락/현재 등
anchors = [(0,18000),(15,17110),(70,28000),(150,80000),(178,78000),(218,112800)]  # 예시: 티엘비
# 가격 범위(차트 y축)와 캔들 색상 규칙만 종목에 맞게 조정
PMIN, PMAX = 15000, 140000          # y축 최저/최고 (라벨과 일치시킬 것)
days=[]
for i in range(len(anchors)-1):
    d0,p0=anchors[i]; d1,p1=anchors[i+1]
    for d in range(d0,d1):
        t=(d-d0)/(d1-d0); days.append(p0+(p1-p0)*t)
days.append(anchors[-1][1])
X0,X1,Yt,Yb = 52,688,10,126
yv=lambda p: Yb-(p-PMIN)/(PMAX-PMIN)*(Yb-Yt)
n=len(days); xv=lambda i: X0+(X1-X0)*i/(n-1); cw=max(1.6,(X1-X0)/n*0.62)
svg=[]; prev=days[0]
for i,close in enumerate(days):
    vol=close*random.uniform(0.02,0.055); op=prev*random.uniform(0.99,1.01)
    hi=max(op,close)+vol*random.uniform(.3,1); lo=max(min(op,close)-vol*random.uniform(.3,1), PMIN)
    up=close>=op; col="var(--accent)" if up else "#7a7070"
    x=xv(i); bt=min(yv(op),yv(close)); bh=max(0.6,abs(yv(close)-yv(op)))
    svg.append(f'<line x1="{x:.1f}" y1="{yv(hi):.1f}" x2="{x:.1f}" y2="{yv(lo):.1f}" stroke="{col}" stroke-width="0.5"/>')
    svg.append(f'<rect x="{x-cw/2:.1f}" y="{bt:.1f}" width="{cw:.1f}" height="{bh:.1f}" fill="{col}"/>')
    prev=close
open('candles.txt','w').write("\n      ".join(svg))
# y라벨 좌표: for p in [PMAX, ...]: print(p, yv(p))  → chartbox 라벨과 일치시킬 것
```

```html
<!-- chartbox에 삽입: {candles}=위 스크립트 출력, y라벨은 PMIN~PMAX에 맞춰 4틱 -->
<div class="chartbox">
  <div class="bt">주가 추이 (<기간>, 일봉 캔들 · 원 · <실제가/수정주가> 기준)</div>
  <svg viewBox="0 0 700 160" xmlns="http://www.w3.org/2000/svg" style="width:100%;height:auto;">
    <line x1="46" y1="10" x2="46" y2="126" stroke="var(--navy)" stroke-width="1.2"/>
    <line x1="46" y1="126" x2="690" y2="126" stroke="var(--navy)" stroke-width="1.2"/>
    <!-- y라벨 4개(text)+격자(line): y=yv(가격) 계산값 사용 -->
    {candles}
    <!-- 주요 마커: 최저/최고/권리락/현재 (text, 필요시 dashed line+rect 말풍선) -->
    <!-- x라벨 5~6개(text), 범례: 상승일(--accent)/하락일(#7a7070) rect+text -->
  </svg>
  <div class="cap"><차트 해설>. 검증된 변곡점 기반 근사 도식이며 실제 일별 시세와 차이가 있을 수 있음.</div>
</div>
```
> **좌표 매핑**: y=126=PMIN, y=10=PMAX. x=52(시작)~688(현재). 상장 3년 미만이면 전체기간.
> **렌더 검증**(픽셀): 차트영역 크롭 후 크림슨(상승)·회색(하락) 픽셀이 모두 있고 잉크 x열이 100+이면 정상.


### 5-4. 재무 요약표
```html
<h3 class="sub">핵심 재무 요약 (연결 기준)</h3>
<table class="data">
  <tr><th>구분</th><th>Y-3</th><th>Y-2</th><th>Y-1</th><th>최근분기</th></tr>
  <tr><td>매출액 (억원)</td>...</tr>
  <tr><td>매출 성장률 (YoY)</td>...</tr>
  <tr class="hl"><td>영업손익 (억원)</td>...</tr>
  <tr><td>당기순손익 (억원)</td>...</tr>
</table>
```

### 5-5. 최근 분기 심층 해석 (강조 박스, 3줄)
```html
<div class="callout" style="border-left-color:var(--accent); background:var(--accent-soft);">
  <b>최근 분기(<기간>) 재무 심층 해석:</b><br>
  ① <매출 구조/비중 통찰: 예 "매출의 80%가 ○○에서 발생"><br>
  ② <성장 vs 수익성: 예 "매출 2배↑이나 R&D 증가로 영업적자"><br>
  ③ <현금흐름/자본조달/이익전망>
</div>
```

### 5-6. 투자 아이디어 카드 (2페이지 하단, Bull/Bear 다음에 배치)
> Executive Summary → Bull/Bear 2단 다음에 `<h2 class="section">핵심 투자 아이디어</h2>`를
> 두고 카드 3~4개를 이어 붙인 뒤, `summbox`로 '성향별 매핑' 종합. 모두 **한 페이지**에 담는다.
```html
<div class="idea">
  <div class="ih"><span class="num">01</span>
    <span class="title"><아이디어 제목></span>
    <span class="horizon"><투자기간 예: 중장기 12~24M></span></div>
  <div class="body"><아이디어 본문·근거></div>
  <div class="kvline"><b>촉매:</b> ... | <b>확인지표:</b> ... | <b>리스크:</b> ...</div>
</div>
<!-- 마지막 역발상/리스크관리 카드는 class="idea rev" (붉은 강조) -->
```

### 5-7. 매트릭스/스코어카드 바
```html
<div class="bar-row"><div class="name"><항목></div>
  <div class="bar-track"><div class="bar-fill" style="width:80%"></div></div>
  <div class="sc">8/10</div></div>
<!-- bar-fill.b 로 파란(갈색)계열 구분 가능 -->
```

### 5-8. 타임라인 (간트차트)
```html
<div class="tl"><div class="tl-track">
  <div class="tl-lane"><div class="lane-name"><사업부문></div>
    <div class="tl-bar-wrap">
      <div class="tl-grid"><div class="cell"></div>×5</div>
      <div class="tl-seg seg-done" style="left:0%;width:38%;">완료단계</div>
      <div class="tl-seg seg-now"  style="left:39%;width:30%;">진행단계</div>
      <div class="tl-seg seg-fut"  style="left:70%;width:28%;">미래단계</div>
    </div></div>
</div>
  <div class="tl-axis"><div class="yr">Y-1</div>...×5</div>
  <div class="tl-legend">
    <span><i style="background:var(--green)"></i>완료·상업화</span>
    <span><i style="background:var(--accent)"></i>현재 진행</span>
    <span><i style="background:#b39a9a"></i>계획·미래</span>
  </div>
</div>
```
> 세그먼트 텍스트는 짧게(좁은 막대에서 잘림 방지, font 6.7px). left/width %로 시점 배치.

### 5-9. 해자 표 / 이벤트 캘린더 / 시나리오
```html
<!-- 해자 -->
<table class="moat"><tr><th>해자 요소</th><th>내용 & 경쟁사 대비</th><th>강도</th></tr>
  <tr><td>①...</td><td>...</td><td class="moat-score ms-strong">强</td></tr></table>

<!-- 이벤트 캘린더: 상승/하락 2표. 영향 열에 class로 색 -->
<td class="green">↑↑ 高</td>   <!-- 상승 -->
<td class="red">↓↓ 高</td>     <!-- 하락, tr에 class="hl"로 최임박 강조 -->

<!-- 시나리오 -->
<tr><td class="green">Bull</td><td>...</td></tr>
<tr><td class="blue">Base</td><td>...</td></tr>
<tr><td class="red">Bear</td><td>...</td></tr>
```

### 5-10. 페이지 헤더 / 섹션 제목 / 콜아웃 / 면책
```html
<div class="pagehdr"><span><종목> (<티커>) · Company Deep-Dive</span>
  <span class="r">01. <페이지 라벨></span></div>
<h2 class="section">Ⅰ. <섹션명></h2>
<h3 class="sub"><소제목></h3>
<div class="callout">일반</div>
<div class="callout pos">긍정</div>
<div class="callout warn">경고</div>
<div class="summbox" style="border-top-color:var(--accent);">
  <div class="h"><소결 제목></div><p>...</p></div>
<!-- 면책은 존댓말 유지 -->
<div class="disc"><b>면책 조항(Disclaimer):</b> 본 문서는 공개된 언론·공시를 종합한 참고용 스터디 자료이며 투자권유가 아닙니다. ... 최신 공시(DART·SEC)로 재확인하십시오. © <연도> 방배동 베어그릴스 Research.</div>
```

### 5-11. Appendix — 사업영역별 진행상황 로그 표 (맨 뒤 부록)
> 좌측=사업영역(크림슨 박스), 우측=시점별 진행상황을 최신순 불릿으로. 각 `<li>`는 자동으로
> 앞에 "–"가 붙고 `.dt`가 시점을 강조함. 사업영역 수만큼 `<tr>` 반복. 첫 행에 `class="recent"`로
> 주력 부문 강조 가능. 파이프라인이 여러 개인 종목(바이오·플랫폼 등)에 특히 유용.
```html
<div class="page">
  <div class="pagehdr"><span><종목> · Company Deep-Dive</span><span class="r">Appendix. 사업영역별 주요 진행상황</span></div>
  <h2 class="section">[Appendix] 사업영역별 주요 진행상황</h2>
  <p>각 사업영역의 핵심 진행 이벤트를 시점 역순(최신 위)으로 정리함. (E)는 기대치로 확정 일정 아님.</p>
  <table class="applog">
    <tr><th style="width:22%">사업영역</th><th>주요 진행상황</th></tr>
    <tr class="recent">
      <td class="seg">1. <사업영역명></td>
      <td><ul class="log">
        <li><span class="dt">(<시점>)</span> <진행 내용></li>
        <li><span class="dt">(<시점>)</span> <진행 내용></li>
        <!-- 최신순으로 여러 줄 -->
      </ul></td>
    </tr>
    <!-- 사업영역 수만큼 <tr> 반복 (2번째부터는 class 없이 <tr>) -->
  </table>
  <div class="disc">본 Appendix의 일자는 보도·공시 기준이며 (E)는 기대·계획임. 실제 진행·결과는 최신 공시로 재확인하십시오.</div>
</div>
```
> CSS는 `report_template.css`의 `table.applog` 블록에 포함됨(`.seg`=사업영역 셀, `ul.log li`=진행 불릿, `.dt`=시점 강조, `tr.recent`=주력 부문 강조).

---

## 6. 분석 품질 기준 (애널리스트 마인드셋)

- **균형**: Bull과 Bear를 항상 병기. 회사 홍보 문구("세계 최초", "혁신")는
  **비판적으로 검증**하고 마케팅 수사와 실증을 구분(예: "FDA 등록 ≠ 효능 승인").
- **분리 사고**: '현재 돈 버는 사업'과 '주가 프리미엄 만드는 사업'을 나눠 설명.
  '서사(narrative)'와 '검증(validation)'을 구분.
- **수치 근거**: 매출 성장률·비중·통계치 등 구체 숫자 인용. 단, 변동 수치는 검색으로 확인.
- **정성 평가 표시**: 스코어카드·시나리오 등 주관 평가는 "애널리스트 주관"임을 명시.
- **저작권**: 기사 문장 그대로 복사 금지. 15단어 미만 인용 1회 이하, 나머지는 패러프레이즈.
- **면책**: 라이선스 보유 자문가가 아니며 투자권유가 아님을 명시. 수치는 최신 공시 재확인 안내.
- **법인격 존중**: 특정인의 심리·의도 단정 금지. 공개 사실에 근거해 서술.

---

## 7. 산출물
- `/mnt/user-data/outputs/<종목>_심층분석리포트.pdf` (최종 결과물)
- 작업용: `report.html`(작업 폴더)
- 스킬 리소스: `report_template.css`(스타일), `build_pdf.py`(HTML→PDF), `gen_candles.py`(일봉 캔들 SVG)

## 8. 체크리스트 (완료 전 self-check)
- [ ] 종목별 최신 주가·시총·지분율·PER을 **검색으로** 확인했는가
- [ ] **주식수 변동 이벤트**(무상·유상증자·권리락) 확인 및 수정주가/실제가 기준 명시했는가
- [ ] 업종 성격에 맞게 페이지를 구성했는가 (제조·부품=경쟁/증설 중심, 바이오=파이프라인/해자 중심)
- [ ] 사업부문을 **빠짐없이** 다뤘는가 (다각화 기업 주의)
- [ ] 표지에 서술형 회사소개 + 회사개요/주주 + **일봉 캔들 주가차트** + 재무 + 심층해석 3줄 포함?
- [ ] **2페이지에 요약·Thesis·투자아이디어를 통합**했는가 (별도 rating strip 스냅샷·리포트구성 콜아웃 없이)?
- [ ] 투자 아이디어 3~4개 + 향후 이벤트 캘린더 포함?
- [ ] 본문 **개조식**(~음/함/임) 변환 완료 (면책은 존댓말 유지)?
- [ ] **한자 0개** 확인 (한글/영어로 대체, 로마숫자 섹션번호는 허용)?
- [ ] **맨 뒤 Appendix**(사업영역·이벤트별 진행상황 로그 표) 포함?
- [ ] 검붉은색 팔레트 적용, 페이지 헤더 연번 정상?
- [ ] 캔들차트 렌더 픽셀 검증(상승/하락 캔들 모두 존재), 페이지 넘침 없음?
- [ ] Bull/Bear 균형, 마케팅 수사·홍보 문구 비판적 검증?
- [ ] 면책조항 포함, PDF 정상 생성 및 present_files 전달?


---

## 부록 A. report_template.css (전문 — <style>...</style> 안에 그대로 사용)

```css

  @page {
    size: A4;
    margin: 14mm 13mm 16mm 13mm;
    @bottom-center { content: counter(page); }
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html { -webkit-print-color-adjust: exact; print-color-adjust: exact; }

  body {
    font-family: 'Noto Sans CJK KR', 'Noto Sans KR', sans-serif;
    font-size: 9.3px;
    line-height: 1.62;
    color: #1c2430;
    background: #fff;
  }

  :root {
    --navy: #5c1418;        /* deep dark red (기존 navy 대체) */
    --navy2: #7d1f24;       /* dark red */
    --accent: #a8323a;      /* crimson accent (기존 gold 대체) */
    --accent-soft: #f5e3e4; /* soft red tint */
    --red: #8f1d18;         /* strong danger red */
    --green: #1e7a48;       /* keep green for positive */
    --blue: #9c4a1b;        /* burnt orange-brown (기존 blue 대체) */
    --grey: #6a6060;
    --line: #e2d5d5;
    --bg-soft: #faf5f5;
  }

  .page { page-break-after: always; }
  .page:last-child { page-break-after: auto; }

  h2.section {
    font-size: 14px;
    color: var(--navy);
    font-weight: 800;
    border-left: 5px solid var(--accent);
    padding: 3px 0 3px 9px;
    margin: 18px 0 9px 0;
    letter-spacing: -0.3px;
  }
  h3.sub {
    font-size: 11px;
    color: var(--navy2);
    font-weight: 700;
    margin: 13px 0 5px 0;
    padding-bottom: 3px;
    border-bottom: 1.5px solid var(--line);
  }
  h4.mini {
    font-size: 9.8px; color: var(--accent);
    font-weight: 700; margin: 9px 0 3px 0;
  }
  p { margin-bottom: 6px; text-align: justify; }
  .em { color: var(--navy); font-weight: 700; }
  .red { color: var(--red); font-weight: 700; }
  .green { color: var(--green); font-weight: 700; }
  .blue { color: var(--blue); font-weight: 700; }

  /* ============ COVER HEADER ============ */
  .masthead {
    display: flex; justify-content: space-between; align-items: flex-start;
    border-bottom: 3px solid var(--navy);
    padding-bottom: 7px; margin-bottom: 3px;
  }
  .brand { font-size: 10px; color: var(--grey); letter-spacing: 2px; font-weight: 700; }
  .brand .firm { color: var(--navy); font-size: 13px; font-weight: 800; letter-spacing: 0.5px; }
  .doctype { text-align: right; font-size: 8px; color: var(--grey); line-height: 1.5; }
  .doctype .tag {
    display:inline-block; background: var(--navy); color:#fff;
    padding: 2px 8px; font-weight:700; letter-spacing:1px; font-size: 8.5px; margin-bottom:3px;
  }

  .titlebar { margin: 12px 0 4px 0; }
  .titlebar .co {
    font-size: 25px; font-weight: 800; color: var(--navy); letter-spacing: -0.6px;
  }
  .titlebar .ticker {
    font-size: 12px; color: var(--accent); font-weight: 700; margin-left: 6px;
  }
  .subtitle {
    font-size: 12px; color: var(--navy2); font-weight: 600; margin-bottom: 12px;
    letter-spacing: -0.3px;
  }

  /* rating strip */
  .ratingstrip {
    display: flex; gap: 0; border: 1px solid var(--line); margin-bottom: 14px;
  }
  .rcell { flex: 1; padding: 8px 10px; border-right: 1px solid var(--line); }
  .rcell:last-child { border-right: none; }
  .rcell .lab { font-size: 7.6px; color: var(--grey); letter-spacing: 1px; font-weight:700; text-transform: uppercase; }
  .rcell .val { font-size: 14px; font-weight: 800; color: var(--navy); margin-top: 2px; }
  .rcell .val.rating { color: var(--accent); }
  .rcell .sub2 { font-size: 7.8px; color: var(--grey); margin-top:1px; }

  /* summary box */
  .summbox {
    background: var(--bg-soft); border: 1px solid var(--line);
    border-top: 3px solid var(--accent);
    padding: 11px 13px; margin-bottom: 12px;
  }
  .summbox .h { font-size: 10.5px; font-weight: 800; color: var(--navy); margin-bottom: 5px; }

  /* two column */
  .cols { display: flex; gap: 14px; }
  .col { flex: 1; }

  /* tables */
  table.data {
    width: 100%; border-collapse: collapse; margin: 6px 0 10px 0; font-size: 8.7px;
  }
  table.data th {
    background: var(--navy); color: #fff; font-weight: 700; padding: 5px 7px;
    text-align: right; border: 1px solid var(--navy);
  }
  table.data th:first-child { text-align: left; }
  table.data td { padding: 4.5px 7px; border: 1px solid var(--line); text-align: right; }
  table.data td:first-child { text-align: left; font-weight: 600; color: var(--navy2); }
  table.data tr:nth-child(even) td { background: #f8fafb; }
  table.data tr.hl td { background: var(--accent-soft); font-weight: 700; }
  table.data .foot td { background: var(--navy2); color:#fff; font-weight:700; }

  ul.clean { list-style: none; margin: 4px 0 8px 0; }
  ul.clean li {
    position: relative; padding-left: 14px; margin-bottom: 4px;
  }
  ul.clean li::before {
    content: "▪"; color: var(--accent); position: absolute; left: 0; top: -1px; font-size: 8px;
  }

  .callout {
    border-left: 3px solid var(--blue); background: #eef4fb;
    padding: 8px 11px; margin: 8px 0; font-size: 8.9px;
  }
  .callout.warn { border-left-color: var(--red); background: #fbeeed; }
  .callout.pos { border-left-color: var(--green); background: #ecf6f0; }

  .kv { font-size: 8.7px; }
  .kv b { color: var(--navy); }

  /* scorecard bars */
  .bar-row { display:flex; align-items:center; margin-bottom: 5px; font-size: 8.6px; }
  .bar-row .name { width: 122px; font-weight:600; color: var(--navy2); }
  .bar-track { flex:1; height: 11px; background: #e9edf1; position: relative; }
  .bar-fill { height: 100%; background: var(--accent); }
  .bar-fill.b { background: var(--blue); }
  .bar-row .sc { width: 34px; text-align:right; font-weight:700; color: var(--navy); }

  .disc {
    font-size: 6.9px; color: var(--grey); line-height: 1.4;
    border-top: 1px solid var(--line); padding-top: 5px; margin-top: 6px; text-align: justify;
  }
  .footer-note { font-size: 7.6px; color: var(--grey); margin-top: 4px; }

  .pagehdr {
    display:flex; justify-content: space-between; font-size: 7.8px; color: var(--grey);
    border-bottom: 1px solid var(--line); padding-bottom: 4px; margin-bottom: 8px; font-weight: 600;
  }
  .pagehdr .r { color: var(--navy); }

  .tag-inline {
    display:inline-block; font-size:7.4px; font-weight:700; padding: 1px 6px;
    border-radius: 2px; margin-left: 4px; vertical-align: middle;
  }
  .t-buy { background: var(--green); color:#fff; }
  .t-hold { background: var(--accent); color:#fff; }
  .t-sell { background: var(--red); color:#fff; }
  .t-na { background: var(--grey); color:#fff; }

  .metric-grid { display:flex; gap:8px; margin: 8px 0; }
  .metric-card {
    flex:1; border:1px solid var(--line); border-top: 2.5px solid var(--navy2);
    padding: 7px 8px; background:#fff;
  }
  .metric-card .lab { font-size:7.4px; color:var(--grey); font-weight:700; letter-spacing:0.5px; }
  .metric-card .num { font-size: 15px; font-weight:800; color:var(--navy); margin: 1px 0; }
  .metric-card .cap { font-size:7.5px; color:var(--grey); }

  /* ===== timeline ===== */
  .tl { position: relative; margin: 8px 0 10px 0; padding-left: 4px; }
  .tl-track { position: relative; }
  .tl-lane {
    display: flex; align-items: center; margin-bottom: 6px;
  }
  .tl-lane .lane-name {
    width: 92px; font-size: 8.3px; font-weight: 700; color: var(--navy2);
    flex-shrink: 0; padding-right: 6px; text-align: right;
  }
  .tl-bar-wrap { flex: 1; position: relative; height: 22px; }
  .tl-grid {
    position: absolute; top: 0; bottom: 0; left: 0; right: 0;
    display: flex;
  }
  .tl-grid .cell { flex: 1; border-left: 1px dashed #e2e7ec; }
  .tl-seg {
    position: absolute; top: 3px; height: 15px; border-radius: 8px;
    font-size: 6.7px; color: #fff; font-weight: 700;
    display: flex; align-items: center; justify-content: center;
    white-space: nowrap; overflow: hidden; padding: 0 2px; letter-spacing: -0.2px;
  }
  .seg-done { background: var(--green); }
  .seg-now  { background: var(--accent); }
  .seg-fut  { background: #b39a9a; }
  .seg-risk { background: var(--red); }
  .tl-axis {
    display: flex; margin-left: 92px; border-top: 1.5px solid var(--navy);
    padding-top: 2px;
  }
  .tl-axis .yr { flex: 1; font-size: 7.4px; color: var(--grey); font-weight: 700; text-align: center; }
  .tl-legend { display:flex; gap: 12px; font-size:7.4px; color:var(--grey); margin-top:4px; margin-left:92px; }
  .tl-legend span { display:inline-flex; align-items:center; gap:3px; }
  .tl-legend i { width:9px; height:9px; border-radius:2px; display:inline-block; }

  /* ===== moat table ===== */
  table.moat { width:100%; border-collapse: collapse; margin:6px 0 8px 0; font-size:8.4px; }
  table.moat th { background: var(--navy); color:#fff; padding:5px 6px; text-align:left; border:1px solid var(--navy); font-weight:700; }
  table.moat td { padding:5px 6px; border:1px solid var(--line); vertical-align: top; }
  table.moat td:first-child { font-weight:700; color:var(--navy2); background:#f8fafb; width:20%; }
  .moat-score { font-weight:800; }
  .ms-strong { color: var(--green); }
  .ms-mid { color: var(--accent); }
  .ms-weak { color: var(--red); }

  /* ===== page1: company overview blocks ===== */
  .ov-grid { display:flex; gap:10px; margin: 10px 0; }
  .ov-box { flex:1; border:1px solid var(--line); border-top:2.5px solid var(--navy2); padding:8px 10px; }
  .ov-box .bt { font-size:8.6px; font-weight:800; color:var(--navy); margin-bottom:5px; letter-spacing:-0.2px; border-bottom:1px solid var(--line); padding-bottom:3px;}
  .ov-row { display:flex; justify-content:space-between; font-size:8.1px; margin-bottom:2.5px; }
  .ov-row .k { color:var(--grey); }
  .ov-row .v { color:var(--navy2); font-weight:700; text-align:right; }
  .chartbox { border:1px solid var(--line); border-top:2.5px solid var(--accent); padding:8px 10px 4px 10px; margin:10px 0; }
  .chartbox .bt { font-size:8.6px; font-weight:800; color:var(--navy); margin-bottom:2px; }
  .chartbox .cap { font-size:7.4px; color:var(--grey); margin-top:2px; }

  /* ===== investment ideas ===== */
  .idea { border:1px solid var(--line); border-left:4px solid var(--accent); margin-bottom:9px; padding:9px 12px; background:#fff; }
  .idea .ih { display:flex; align-items:baseline; gap:8px; margin-bottom:4px; }
  .idea .num { font-size:14px; font-weight:800; color:var(--accent); line-height:1; }
  .idea .title { font-size:10.5px; font-weight:800; color:var(--navy); letter-spacing:-0.3px; }
  .idea .horizon { margin-left:auto; font-size:7.3px; font-weight:700; color:#fff; background:var(--navy2); padding:2px 7px; border-radius:2px; white-space:nowrap; }
  .idea .body { font-size:8.7px; margin-bottom:4px; }
  .idea .kvline { font-size:8px; color:var(--grey); border-top:1px dotted var(--line); padding-top:4px; }
  .idea .kvline b { color:var(--navy2); }
  .idea.rev { border-left-color: var(--red); }
  .idea.rev .num, .idea.rev .title span.hl2 { color: var(--red); }

  /* ===== appendix: 사업영역별 진행상황 표 ===== */
  table.appx { width:100%; border-collapse:collapse; margin:8px 0; font-size:8.5px; }
  table.appx td { border:1px solid var(--line); padding:7px 9px; vertical-align:top; }
  table.appx td.area {
    width:22%; background:var(--accent-soft); font-weight:800; color:var(--navy);
    font-size:9.3px; letter-spacing:-0.2px;
  }
  table.appx td.prog { line-height:1.7; }
  table.appx td.prog .ln { display:block; margin-bottom:3px; padding-left:12px; position:relative; }
  table.appx td.prog .ln::before { content:"–"; position:absolute; left:0; color:var(--accent); font-weight:700; }
  table.appx td.prog .dt { font-weight:700; color:var(--navy2); }
  table.appx tr:nth-child(even) td.area { background:#f3dcdd; }

  /* ===== appendix: progress log table ===== */
  table.applog { width:100%; border-collapse:collapse; margin:8px 0 12px 0; font-size:8.5px; }
  table.applog th {
    background:var(--navy); color:#fff; font-weight:700; padding:6px 8px;
    text-align:left; border:1px solid var(--navy);
  }
  table.applog td { padding:7px 9px; border:1px solid var(--line); vertical-align:top; }
  table.applog td.seg {
    width:22%; font-weight:800; color:var(--navy); background:var(--accent-soft);
    font-size:9px; vertical-align:middle;
  }
  table.applog ul.log { list-style:none; margin:0; padding:0; }
  table.applog ul.log li {
    position:relative; padding-left:12px; margin-bottom:4px; line-height:1.5;
  }
  table.applog ul.log li:last-child { margin-bottom:0; }
  table.applog ul.log li::before {
    content:"–"; position:absolute; left:0; color:var(--accent); font-weight:700;
  }
  table.applog .dt { font-weight:800; color:var(--navy2); }
  table.applog tr.recent td.seg { background:var(--navy2); color:#fff; }

  /* ===== appendix: 사업영역별 진행상황 표 ===== */
  table.appx { width:100%; border-collapse:collapse; margin:8px 0; font-size:8.5px; }
  table.appx td { border:1px solid var(--line); padding:7px 9px; vertical-align:top; }
  table.appx td.area {
    width:22%; background:var(--accent-soft); font-weight:800; color:var(--navy);
    font-size:9.3px; letter-spacing:-0.2px;
  }
  table.appx td.prog { line-height:1.7; }
  table.appx td.prog .ln { display:block; margin-bottom:3px; padding-left:12px; position:relative; }
  table.appx td.prog .ln::before { content:"–"; position:absolute; left:0; color:var(--accent); font-weight:700; }
  table.appx td.prog .dt { font-weight:700; color:var(--navy2); }
  table.appx tr:nth-child(even) td.area { background:#f3dcdd; }

```

---

## 부록 B. build_pdf.py (HTML→PDF 변환 스크립트)

```python
#!/usr/bin/env python3
"""
Equity Report PDF Builder
사용법: python3 build_pdf.py <input_html> <output_pdf>
예)     python3 build_pdf.py report.html /mnt/user-data/outputs/삼성전자_심층분석리포트.pdf

- Chromium(Playwright) 렌더링을 우선 사용(최신 CSS·flex·grid 완벽 지원).
- Chromium 미설치 시 자동으로 설치를 시도하며, 실패하면 wkhtmltopdf로 폴백.
"""
import sys, os, glob, subprocess

def find_chromium():
    cands = glob.glob("/opt/pw-browsers/chromium-*/chrome-linux/chrome")
    return cands[0] if cands else None

def build_with_playwright(html_path, out_pdf, chrome_path):
    from playwright.sync_api import sync_playwright
    url = "file://" + os.path.abspath(html_path)
    with sync_playwright() as p:
        browser = p.chromium.launch(executable_path=chrome_path)
        page = browser.new_page()
        page.goto(url, wait_until="networkidle")
        page.pdf(path=out_pdf, format="A4", print_background=True,
                 prefer_css_page_size=True)
        browser.close()

def main():
    if len(sys.argv) < 3:
        print("사용법: python3 build_pdf.py <input_html> <output_pdf>")
        sys.exit(1)
    html_path, out_pdf = sys.argv[1], sys.argv[2]
    os.makedirs(os.path.dirname(out_pdf) or ".", exist_ok=True)

    chrome = find_chromium()
    if chrome is None:
        # Chromium 설치 시도
        try:
            subprocess.run(["pip", "install", "playwright", "--break-system-packages", "-q"], check=False)
            subprocess.run(["playwright", "install", "chromium"], check=False, timeout=240)
            chrome = find_chromium()
        except Exception as e:
            print("Chromium 설치 실패:", e)

    if chrome:
        build_with_playwright(html_path, out_pdf, chrome)
        print("PDF 생성 완료(Chromium):", out_pdf, os.path.getsize(out_pdf), "bytes")
    else:
        # 폴백: wkhtmltopdf (일부 최신 CSS 미지원 가능)
        subprocess.run(["wkhtmltopdf", "--enable-local-file-access",
                        "--page-size", "A4", html_path, out_pdf], check=True)
        print("PDF 생성 완료(wkhtmltopdf 폴백):", out_pdf)

if __name__ == "__main__":
    main()

```

---

## 부록 C. gen_candles.py (일봉 캔들 SVG 생성기)

```python
#!/usr/bin/env python3
"""
gen_candles.py — 변곡점 보간 방식의 일봉 캔들차트 SVG 생성기

실제 일별 OHLC 전체 시계열은 공개데이터로 확보하기 어렵기 때문에,
검색으로 확인한 주요 변곡점(상장가·52주 최저/최고·급등/급락·현재가 등)을
선형 보간해 '일봉 밀도'(약 200~250개 캔들)의 근사 캔들차트를 생성한다.

사용법:
  1) 아래 ANCHORS, PMIN, PMAX 를 종목 데이터에 맞게 수정
  2) python3 gen_candles.py  → candles.txt (SVG 조각) 생성 + y라벨 좌표 출력
  3) candles.txt 내용을 report.html 의 <svg> chartbox 안에 삽입
  4) 출력된 y라벨/마커 좌표를 차트 라벨과 일치시킨다

주의(주식수 변동): 무상증자·유상증자·권리락이 있으면
  - '실제 거래가' 기준으로 그리고 권리락 지점에 마커를 찍거나
  - '수정주가'로 통일하되 캡션에 기준을 명시. 절대 혼용하지 말 것.
"""
import random
random.seed(7)

# ===== 종목별로 수정하는 부분 =====
# (거래일 인덱스, 종가) — 검색으로 확인한 변곡점. 인덱스는 거래일 순번(약 21일/월).
ANCHORS = [
    (0,   18000),
    (15,  17110),   # 예: 52주 최저
    (70,  28000),
    (150, 80000),   # 예: 급등 고점
    (178, 78000),   # 예: 권리락 등 이벤트
    (218, 112800),  # 최근가
]
PMIN, PMAX = 15000, 140000   # y축 최저/최고 (차트 라벨과 반드시 일치)
# =================================

# 플롯 영역 좌표
X0, X1, Y_TOP, Y_BOT = 52, 688, 10, 126
yv = lambda p: Y_BOT - (p - PMIN) / (PMAX - PMIN) * (Y_BOT - Y_TOP)

# 변곡점 선형보간 → 일별 종가
days = []
for i in range(len(ANCHORS) - 1):
    d0, p0 = ANCHORS[i]; d1, p1 = ANCHORS[i + 1]
    for d in range(d0, d1):
        t = (d - d0) / (d1 - d0)
        days.append(p0 + (p1 - p0) * t)
days.append(ANCHORS[-1][1])

n = len(days)
xv = lambda i: X0 + (X1 - X0) * i / (n - 1)
cw = max(1.6, (X1 - X0) / n * 0.62)   # 캔들 body 폭

svg = []
prev = days[0]
for i, close in enumerate(days):
    vol = close * random.uniform(0.02, 0.055)
    op = prev * random.uniform(0.99, 1.01)
    hi = max(op, close) + vol * random.uniform(0.3, 1.0)
    lo = max(min(op, close) - vol * random.uniform(0.3, 1.0), PMIN)
    up = close >= op
    col = "var(--accent)" if up else "#7a7070"
    x = xv(i)
    bt = min(yv(op), yv(close)); bh = max(0.6, abs(yv(close) - yv(op)))
    svg.append(f'<line x1="{x:.1f}" y1="{yv(hi):.1f}" x2="{x:.1f}" y2="{yv(lo):.1f}" stroke="{col}" stroke-width="0.5"/>')
    svg.append(f'<rect x="{x-cw/2:.1f}" y="{bt:.1f}" width="{cw:.1f}" height="{bh:.1f}" fill="{col}"/>')
    prev = close

with open("candles.txt", "w") as f:
    f.write("\n      ".join(svg))

print(f"candles: {n}, body width: {cw:.2f}  → candles.txt 생성")
print("\n[y라벨 좌표 — chartbox 라벨과 일치시킬 것]")
step = (PMAX - PMIN) / 4
for k in range(5):
    p = PMAX - step * k
    print(f"  {int(p):>8,}원  →  y={yv(p):.1f}")
print("\n[주요 마커 좌표 — 종목 변곡점]")
for d, p in ANCHORS:
    idx = min(range(n), key=lambda k: abs(k - d))
    print(f"  day{d} {int(p):>8,}원  →  x={xv(idx):.0f} y={yv(p):.1f}")

```
